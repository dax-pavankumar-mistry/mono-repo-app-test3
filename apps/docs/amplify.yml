version: 1
applications:
  - appRoot: apps/docs
    frontend:
      phases:
        preBuild:
          commands:
            - yarn install --frozen-lockfile
        build:
          commands:
            - yarn workspace docs build
      artifacts:
        baseDirectory: apps/docs/.next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - ../node_modules/**/*
          - .turbo/**/*
    source:
      buildFilter:
        include:
          - apps/docs/**
          - packages/ui/**
          - packages/config/**
        exclude:
          - apps/web/**
